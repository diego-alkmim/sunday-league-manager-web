import { Card } from '../components/ui/Card';

export default function Teams() {
  return (
    <div className="space-y-3">
      <h1 className="text-2xl font-semibold">Times</h1>
      <Card>CRUD de times do usuário (owner) via /teams.</Card>
    </div>
  );
}
